name: ${name}
version: '${version}'
main: me.duart.blackjackLite.BlackjackLite
api-version: '${apiVersion}'
prefix: BlackjackLite
authors: ${authors}
description: ${description}

dependencies:
  server:
    Vault:
      load: AFTER
      required: true